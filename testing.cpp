#include <bits/stdc++.h>
using namespace std;

int main() {
	int x, y, m, n;
	cin >> x >> y;
	int arr1[x][y];
	for (int i = 0; i < x; i++) {
		for (int j = 0; j < y; j++) {
			cin >> arr1[i][j];
		}
	}


	cin >> m >> n;
	int arr2[m][n];
	for (int i = 0; i < x; i++) {
		for (int j = 0; j < y; j++) {
			int a, b;
			// cin >> a >> b;
			cin >> arr2[i][j];
		}
	}


	// code to multiply both matrices

	for (int i = 0; i < x; i++) {
		for (int j = 0; j < y; j++) {
			int a, b;
			// cin >> a >> b;
			// arr2[i][j] = arr2[i][j]*;
		}
	}





	return 0;
}

// Submitted by apkharsh