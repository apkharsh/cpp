#include <bits/stdc++.h>
using namespace std;
int main()
{
	std::ios::sync_with_stdio(false); // ignore this
	int T;
	cin>>T;
	// cin.ignore(); must be there when using getline(cin, s)
	while(T--)
	{
		int N,x,y;
		cin >> N>>x>>y;

		int Xsteps = (N+1)/2;
		int Ysteps = (N+1)/2;

		if((x%Xsteps)==0 && (y%Ysteps)==0){
			if(x>str)

		}


	}
	return 0;
}

// contributed by apkharsh
