#include <bits/stdc++.h>
using namespace std;

// old methods or iterators
// int main() {
// 	// vector<int> v;
// 	// int n;
// 	// cin >> n;

// 	// for (int i = 0 ; i < n; i++) {
// 	// 	v.push_back(i);
// 	// }

// 	// vector<int>::iterator it = v.begin();

// 	// for (it = v.begin(); it != v.end(); it++) {
// 	// 	cout << (*it) << endl;
// 	// }

// 	// it++ and it+1 are different things as it++ will increment 1 value of the iterator while it+1 will point the next memory block. In case of vector ext memory block and next value has the same location so there is no problem here but in case of maps etc. where data is distributed then we cant shoudnt consider them common.

// 	vector<pair<int, string>> V_pair = {{1, "harsh"}, {2, "Harjyot"}};

// 	vector<pair<int, string>>::iterator it;
// 	for (it = V_pair.begin(); it != V_pair.end(); it++) {
// 		cout << (*it).first << " " << (*it).second << endl;
// 	}

// 	for (it = V_pair.begin(); it != V_pair.end(); it++) {

// 		cout << (it->first) << " " << (it->second) << endl;
// 	}




// 	return 0;
// }

// Submitted by apkharsh



// short methods for iteration

int main(){

}